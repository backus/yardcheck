#!/usr/bin/env ruby
# frozen_string_literal: true

require 'yardcheck'

# currently a hack
$LOAD_PATH.unshift(File.expand_path(File.join('../test_app/lib')))
require 'test_app'

Yardcheck::Runner.run(ARGV).check
